# Live OUTPUT Folder Display — 1.10.5

The Client now reads `outputFolder` directly from the Main App dashboard and refreshes it on the normal dashboard polling cycle. Older Main Apps remain supported through the existing report-health destination fallback.
